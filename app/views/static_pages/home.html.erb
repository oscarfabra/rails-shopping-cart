<% if user_logged_in? %>
  <!-- Display user home page. -->
  <%= render 'static_pages/customer_home_page' %>
<% else %>
  <!-- Display landing page. -->
  <%= render 'static_pages/landing_page' %>
<% end %>