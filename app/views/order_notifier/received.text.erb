Dear <%= @order.name %>,

Thank you for your recent order from the Rails Depot App.

You ordered the following items:

<%= render @order.line_items # calls helper -%>

We'll send you a separate e-mail when your order ships.